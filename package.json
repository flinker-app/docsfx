{
  "scripts": {
    "watch": "node scripts/watch-docs.js",
    "build": "node scripts/generate-last-updated.js && docfx build"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "chokidar": "^3.6.0"
  }
}
